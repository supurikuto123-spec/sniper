{
  "name": "pump-sniper-paper",
  "version": "1.0.0",
  "description": "Pump.fun Paper Trading Sniper Bot - All-in-One",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\" --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
